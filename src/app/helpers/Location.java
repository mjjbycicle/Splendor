package app.helpers;

import lib.math.Vec2;

public class Location {
    public static Vec2 getLocation(FinalLocation loc) {
        return switch (loc) {
            case BUTTON_1 -> new Vec2()
        }
    }
}
